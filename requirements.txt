# 读音频/视频标签与容器元数据
mutagen>=1.46.0

# comfyui自带 torch / numpy / Pillow / safetensors / av 

# 提示词助手「设置·llama设置 → 进程内（llama-cpp-python）」
# pip install llama-cpp-python   

# 读取 .gguf / .onnx 模型文件时需要           
# pip install gguf onnx
